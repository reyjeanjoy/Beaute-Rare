<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/pink_background"
    android:padding="16dp"
    android:layout_gravity="center">

    <!-- Title -->
    <TextView
        android:id="@+id/details_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Appointment Details"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FF0080"
        android:layout_marginBottom="20dp" />

    <!-- Customer Name -->
    <TextView
        android:id="@+id/customer_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="Customer Name: John Doe"
        android:textColor="#333"
        android:background="@drawable/text_field_background"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Makeup Name -->
    <TextView
        android:id="@+id/makeup_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="Makeup Name: Smoky Eyes"
        android:textColor="#333"
        android:background="@drawable/text_field_background"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Makeup Details -->
    <TextView
        android:id="@+id/makeup_details"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="Makeup Artist: Jane Smith"
        android:textColor="#333"
        android:background="@drawable/text_field_background"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Appointment Time -->
    <TextView
        android:id="@+id/appointment_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="Appointment Time: 3:00 PM"
        android:textColor="#333"
        android:background="@drawable/text_field_background"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Appointment Date -->
    <TextView
        android:id="@+id/appointment_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="Appointment Date: 2024-12-08"
        android:textColor="#333"
        android:background="@drawable/text_field_background"
        android:padding="8dp"
        android:layout_marginBottom="24dp" />

    <!-- Proceed Button -->
    <Button
        android:id="@+id/proceedBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Next"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:backgroundTint="#FFBCD9"
        android:paddingHorizontal="32dp"
        android:paddingVertical="12dp" />
</LinearLayout>
